

export interface Posts {
    title: string,
    body: string,
    id: number
};

export interface Todos {
    id: number,
    title: string,
    completed: boolean
};

export interface Users {
    name: string,
    username: string,
    id: number
};